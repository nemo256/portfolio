import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
  config: {
    bgColor: 'black',
  },
  fonts: {
    heading: 'Roboto Mono',
    body: 'Roboto Mono',
  },
})

export default theme
